# Development Notes

This directory holds developer-facing handoff and continuity notes that should not live in the repository root.

- `continuation-guide.md`: current runtime baseline, key commands, output locations, and recommended next work.
- `progress.md`: compact activity log for the most recent implementation slice.

The long-lived project entry points remain:

- native source and canonical Qt project: `native/`
- Python reference implementation: `src/comfort_cues/`
- automation and build scripts: `scripts/`
- user-facing runtime validation guide: `docs/native_manual_smoke.md`

